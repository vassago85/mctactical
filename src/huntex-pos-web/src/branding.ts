/** White text + orange — use on dark backgrounds (app shell, login). */
export const logoLight = '/MCTactical Light.png'

/** Black text + orange — favicon, PWA icons, light / high-contrast surfaces. */
export const logoDark = '/MCtactical Dark.png'
